public enum Type {
    VAR,
    NUMBER,
    ADD,
    MIN,
    MULT,
    DIV,
    ASSIGN,
    LEFTPAR, RIGHTPAR,
    SEMICOLON,
    PRINT,
    IF, 
    THEN,
    ELSE,
    GT, 
    LT, 
    EQ, 
    NEQ, 
    GTE, 
    LTE,
    END
} 
